name: wsaa
description: WSAA app with SOS, location, and safety features.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev



environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.1.1
  firebase_auth: ^5.1.1
  cloud_firestore: ^5.0.2
  firebase_messaging: ^15.0.2
  firebase_storage: ^12.1.0
  geolocator: ^12.0.0
  url_launcher: ^6.3.0
  google_maps_flutter: ^2.7.0
  shake: ^2.2.0
  camera: ^0.11.0+1
  audioplayers: ^6.0.0
  volume_watcher: ^2.0.1